# Fires an OSX notification containing a given string after a given period of time in seconds

## Usage:
`python main.py 'this is the string to be displayed in the notification' <time in seconds - must be an integer>`
